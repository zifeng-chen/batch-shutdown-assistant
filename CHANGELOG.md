# 更新日志

## v1.2.0 (2026-09-15)

### 改进
- **统一文件路径**：所有文件归入 `C:\Program Files\LanAgent\`，子目录结构为 `logs\`（日志）、`data\`（数据+文件分发）
- **自动迁移**：升级时自动将旧版 `C:\LanAgent\` 下的文件迁移到新路径，迁移完成后删除旧目录
- **彻底卸载**：卸载时清理安装目录和旧版残留目录，不留任何项目文件
- **管理端版本号**：侧边栏显示当前管理端版本号 v1.2.0

## v1.1.1 (2026-09-15)

### 新增
- **推送升级**：管理端一键推送新版 Agent，支持选择全部/指定设备
- **文件分发**：向被管理端推送文件到指定目录
- **使用帮助页面**：Web 管理端内置完整操作指南和常见问题
- **版本号嵌入 exe**：通过 ldflags 编译时注入，不再依赖外部 version.txt
- **仪表盘增强**：显示服务端最新版本、已安装最高版本、待升级设备数、版本分布、最近操作日志
- **设备列表分区**：在线设备和离线设备分两个区块显示

### 修复
- **心跳超时检测失效**：数据库状态值大小写不匹配（ONLINE vs online）导致离线设备无法被标记，改为原生 SQL 修复
- **升级后服务未重启**：Agent 停掉自身后无法继续执行替换操作，改为独立 cmd 进程完成停服务→替换→启服务
- **deploy.conf 写入 localhost**：从本机浏览器访问管理端时生成的安装包 server 地址为 localhost，目标机器无法连接。改为自动检测宿主机局域网 IP
- **批量删除失败**：task_results 外键约束导致删除设备时报错，改为先清理关联记录再删除设备
- **同包多机 token 冲突**：同一安装包的 token 相同导致 UNIQUE 约束冲突，改为每台设备自动生成唯一 UUID token
- **WebSocket 认证缺失**：Agent WebSocket 连接无身份验证，任何人可冒充设备，增加 token query 参数校验
- **unattend.xml XML 注入**：用户名密码未转义可能破坏 XML 结构，增加 xmlEscape 处理
- **JWT 过期时间偏差**：JWT expire 误用北京时间导致 token 提前8小时过期，改回 UTC

### 改进
- 心跳间隔从 30 秒缩短为 5 秒
- 离线检测阈值设为 15 秒（3次心跳未收到即离线）
- 卸载时删除安装目录和日志目录
- 部署包动态检测宿主机 IP，换机器无需改代码
- 设备列表按 IP 地址数值排序
- 所有时间显示统一为北京时间 YYYY-MM-DD HH:mm:ss 格式
- 前端静态资源加 no-cache 头防止浏览器缓存旧版本
